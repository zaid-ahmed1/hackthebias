<h1>Join a Team</h1>
<p>Looking for teammates? Fill out this form or browse our team finder to connect with others.</p>

<%= form_with url: join_team_path, method: :post, local: true do |form| %>
    <div>
        <%= form.label :name, "Your Name" %>
        <%= form.text_field :name, required: true %>
    </div>
    <div>
        <%= form.label :email, "Email" %>
        <%= form.email_field :email, required: true %>
    </div>
    <div>
        <%= form.label :team_interest, "Team or Role You're Interested In" %>
        <%= form.text_field :team_interest %>
    </div>
    <div>
        <%= form.submit "Join Team" %>
    </div>
<% end %>